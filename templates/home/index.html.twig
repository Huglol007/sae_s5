{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}


<!-- Contenu principal -->
<div class="container mt-5">
    <div class="text-center">
        <h1 class="mb-4">Bienvenue sur la page d'accueil !</h1>
        <p>Utilisez les boutons ci-dessous pour accéder aux différentes fonctionnalités.</p>
    </div>

    <!-- Boutons de navigation -->
    <div class="row mt-4">
        <div class="col-md-4">
            <a href="{{ path('app_jour_non_ouvre_index') }}" class="btn btn-danger w-100">🚫 Gérer les jours non ouvrés</a>
        </div>
    </div>
    <div class="row mt-4">
        <div class="col-md-4">
            <a href="{{ path('admin_ressource_index') }}" class="btn btn-primary w-100">📚 Gérer les ressources</a>
        </div>
    </div>
    <div class="row mt-4">
        <div class="col-md-4">
            <a href="{{ path('app_ressource_semaine_index') }}" class="btn btn-success w-100">📅 Gérer la répartition des heures</a>
        </div>
    </div>
    <div class="row mt-4">
        <div class="col-md-4">
            <a href="{{ path('app_enseignant_index') }}" class="btn btn-warning w-100">👨‍🏫 Gestion des professeurs</a>
        </div>
    </div>
    <div class="row mt-4">
        <div class="col-md-4">
            <a href="{{ path('app_matiere_index') }}" class="btn btn-secondary w-100">🖊️ Gestion des Matières</a>
        </div>
    </div>
    
    <!-- Nouveau bouton pour la gestion des utilisateurs -->
    <div class="row mt-4">
        <div class="col-md-4">
            <a href="{{ path('app_user_index') }}" class="btn btn-info w-100">👥 Gestion des utilisateurs</a>
        </div>
    </div>

</div>

{% endblock %}
