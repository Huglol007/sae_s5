<form method="post" action="{{ path('app_ressource_semaine_delete', {'id': ressource_semaine.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ressource_semaine.id) }}">
    <button class="btn">Delete</button>
</form>
